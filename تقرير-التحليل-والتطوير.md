# تقرير التحليل والتطوير — موقع «سبائك الفخامة»

> دراسة كاملة للموقع الأصلي، وبحث وتحقّق من الأدوات المجانية، ثم إعادة بناء شاملة
> للأداء والتصميم والذكاء والسلاسة. — 19 أغسطس 2026

---

## 1) ملخص تنفيذي

الموقع الأصلي (ملف `index.html` واحد + أصول) كان أساساً متيناً: لوحة عربية RTL
بألوان فخامة ذهبية داكنة، بيانات حقيقية من `gold-api.com` مع خادم وسيط
(Cloudflare Worker) يحفظ سجلاً لـ24 ساعة، وذاكرة محلية. لكنه كان **إصداراً أولياً**
تعاني منه عدة نقاط: سلسلة احتياط ضعيفة، عدم وجود وضع دون اتصال، مخطط بمدى واحد،
لا حاسبة أسعار، وعدة أخطاء برمجية دقيقة.

**بعد التطوير** أصبح الموقع:

- **4 مصادر أسعار** بدل مصدرين، مع رفض تلقائي للقراءات الشاذة والقديمة.
- **مخطط بأربعة مديات** (منذ الفتح / 24 ساعة / 7 أيام / 30 يوم) تُجمع بياناته تلقائياً.
- **PWA كاملة**: تثبيت على الجهاز، عمل دون اتصال، أيقونات، صورة مشاركة OG.
- **حاسبة أسعار فورية** (جرام/أونصة/تولة × 5 عيارات) + سعر الفضة + نسبة الذهب/الفضة.
- **سلاسة بصرية**: عدّادات متحركة، توهج اتجاه الحركة، شريط أسعار متحرك، حلقة عدّ تنازلي للتحديث.
- **اختبار آلي**: 37 اختباراً للوضع الطبيعي + 9 لوضع الفشل، كلها ناجحة بلا أخطاء.

---

## 2) تحليل الموقع الأصلي

### نقاط القوة (حافظنا عليها)
1. **هوية بصرية مميزة**: الأخضر الزمردي العميق مع الذهبي، زخرفة الماس، شعار واضح.
2. **بنية أحادية الملف**: سهلة النشر على GitHub Pages وأي استضافة ثابتة.
3. **بيانات حقيقية**: Worker يخزن نقاط 24 ساعة على فترات 5 دقائق (تحقّقنا من أنها حيّة).
4. **صلابة معقولة**: رفض القراءات الشاذة (`outlier_quote`) والقديمة (`older_quote`).
5. **ذكاء العرض**: فجوات السوق لا تُوصل بخط، وعملة وأرقام بخطوط ثابتة العرض (tabular-nums).

### المشاكل والأخطاء المكتشفة (بالأدلة)
| # | المشكلة | الدليل / الموضع | الأثر |
|---|---------|-----------------|-------|
| 1 | **لا رسم للجلسة أبداً** | `if(!WORKER_API)drawChart(saveSession(q),false)` — و`WORKER_API` له قيمة افتراضية دائماً، فالشرط لا يتحقق أبداً | إذا تعطل الـWorker يبقى المخطط فارغاً رغم وجود نقاط جلسة |
| 2 | **مصدر احتياطي واحد فقط فعلياً** | عند فشل الـWorker يُجرب `gold-api.com` فقط ثم يستسلم | نقطة فشل واحدة |
| 3 | **لا وضع دون اتصال** | لا Service Worker ولا Manifest | إغلاق التبويب/انقطاع النت = شاشة فارغة عند إعادة الفتح |
| 4 | **مخطط بمدى واحد** (24 ساعة فقط) | بيانات الجلسة والتراكم المحلي غير مستغلة | المستخدم لا يرى اتجاه 7/30 يوماً |
| 5 | **`sabaaek-official-logo.png` ملف JPEG بامتداد PNG** | فحصنا الترويسة: `FFD8FFE0` = JPEG داخل ملف `*.png` | فخ مستقبلي لأي أداة/متصفح صارم |
| 6 | **أيقونة متصفح غائبة** (لا favicon) | `<head>` بلا أي `rel=icon` | تبويب الموقع بلا هوية |
| 7 | **لا وسوم Open Graph للصورة** | `og:image` غير موجودة | المشاركة في واتساب/تويتر بلا صورة جذابة |
| 8 | **تحديث عدواني عند العودة للتبويب** | `visibilitychange` + `focus` + `online` مع `refresh(true)` | إهدار طلبات، وإمكانية إزعاج |
| 9 | **لا تراجع عند فشل المصادر** | فشل متكرر = محاولات كل 30 ثانية بلا تهدئة | استهلاك وضغط على الشبكة |
| 10 | **نص المشاركة بلا واتساب مباشر** | `navigator.share`/`clipboard` فقط | في سوق سعودي، واتساب هو القناة الأولى |
| 11 | **لا حاسبة أسعار** رغم أن الموقع لمتجر مجوهرات | — | الزائر لا يحسب سعر وزنه بنفسه |
| 12 | **بطاقات عيارات بلا إشارة اتجاه** | — | لا يرى المتصفح هل ارتفع/انخفض كل عيار |
| 13 | **شعار 186KB يُحمَّل كما هو** | `brand-logo` يستخدم PNG الأصلي 720px | ~120KB مهدرة (حُلّت بـWebP 64KB) |
| 14 | **لا تلميحات لاختصارات أو تثبيت** | — | ميزات PWA غير معروفة للمستخدم |
| 15 | **أخطاء محتملة في أداة التلميح** | النقاط المدمجة من الـWorker تحمل `ts` بينما الكود يقرأ `sourceUpdatedAt` | "Invalid time value" عند التحويم |
| 16 | **`e.target.matches` دون حماية** | في معالج لوحة المفاتيح | كسر عند ضغط مفتاح والتركيز على `document` |
| 17 | **شريط الحالة بلا تفاصيل المصدر الثانوي** | — | لا شفافية عند التحويل الاحتياطي |

---

## 3) البحث والتحقق — أدوات ومشاريع مجانية بالكامل (متحقق منها حياً)

### واجهات أسعار ذهب (لا تحتاج مفتاحاً — جرّبناها جميعاً وقرأنا ردودها)

1. **gold-api.com** — `https://api.gold-api.com/price/XAU/USD`
   مجاني وغير محدود، يدعم CORS من المتصفح، ويشمل XAU وXAG.
   تحقق حي: `{"price":4358.0,"symbol":"XAU","currency":"USD","updatedAt":"2026-08-19T02:36:06Z"}` ✅

2. **goldprice.dev** — `https://api.goldprice.dev/v1/prices?symbol=XAU-USD-SPOT`
   مجاني بلا مفتاح (~100 طلب/ساعة/IP)، يمنح bid/ask و`is_stale`، ويشمل XAG،
   وله صفحة مقارنة صريحة مع gold-api.com. تحقق حي: الاستجابة `symbols[0]` موثقة في [الوثائق](https://goldprice.dev/docs/quickstart) ✅

3. **xaus.com** — `https://xaus.com/api/v1/spot`
   مجاني بلا مفتاح، رد واحد يتضمن XAU + XAG + **نسبة الذهب/الفضة** + أسعار صرف (SAR=3.75).
   تحقق حي: `"gold_silver_ratio":69.01,"fx_rates":{"SAR":3.75}` ✅

4. **تغذية Swissquote العامة** — `https://forex-data-feed.swissquote.com/public-quotes/bboquotes/instrument/XAU/USD`
   مجانية وبلا مفتاح، bid/ask لحظي من وسيط حقيقي (تحقق حي: bid 4355.8 / ask 4356.5).
   **لا تنصح باستخدامها مباشرة من المتصفح** (غياب CORS) — الأفضل إضافتها للـWorker كمصدر رابع. ✅

5. **goldapi.io** — خطة Sandbox مجانية 100 طلب/شهر (مرجع فقط، لا نحتاجها حالياً).

6. مرجع مجتمعي مفيد: نقاش [r/webdev عن واجهات الذهب المجانية](https://www.reddit.com/r/webdev/comments/axkvck/is_there_any_free_api_for_gold_price/) يذكر Swissquote وmetalpriceapi.

### أدوات مجانية للبناء (طُبقت في هذا المشروع)
- **Service Worker + Manifest**: قالب [kosamari لـ GitHub Pages](https://gist.github.com/kosamari/7c5d1e8449b2fbc97d372675f16b566e) — بنينا نسخة معدلة بإستراتيجية "الشبكة أولاً للصفحة / الذاكرة أولاً للأصول".
- **Google Fonts**: خط [IBM Plex Sans Arabic](https://fonts.google.com/specimen/IBM+Plex+Sans+Arabic) المجاني مع `display=swap` (بديل نظامي فوري عند بطء الشبكة).
- **WebP**: ضغط الشعار من 186KB إلى 64KB بجودة 82% عبر `sharp` (بدون خسارة ملحوظة).
- **اختبار تلقائي**: `jsdom` (بيئة DOM في Node) — استخدمناها لمحاكاة المتصفح واختبار كل الوظائف (37+9 اختباراً).

---

## 4) ما تم تنفيذه (بالتفصيل)

### أ. البيانات والذكاء
- **سلسلة احتياط من 4 مصادر** بالترتيب: الـWorker ← gold-api.com ← goldprice.dev ← xaus.com، مع إشعار واحد فقط عند تبديل المصدر (وليس كل 30 ثانية).
- **رفض تلقائي**: قراءات خارج نطاق 1000–20000$، أو أقدم من المقبولة، أو منحرفة >20% عن سابقتها.
- **تراكم محلي ذكي**: كل قراءة تُحفظ (تباعد ≥45 ثانية، سقف 6000 نقطة) → تُبنى مديات 7 و30 يوماً تلقائياً، مع **بذر** السلسلة المحلية بنقاط الـWorker عند أول زيارة.
- **تغطية صادقة**: المخطط يكتب «التغطية الحالية 3 ساعات من 24 — تكتمل تلقائياً» بدل إيهام المستخدم.
- **فضة + نسبة الذهب/الفضة**: تُجلب مع القراءة (أو من gold-api/goldprice.dev) وتُخزَّن 30 دقيقة.
- **تراجع تدريجي (backoff)**: عند فشل المصادر: 30←60←120 ثانية، ويعود فوراً عند نجاح أي مصدر.
- **تحديث موفّر**: إيقاف المؤقت عند إخفاء التبويب، وتحديث فوري عند العودة فقط إذا مرّ >15 ثانية.

### ب. العرض والتصميم
- **عدّاد متحرك** (easeOutCubic، 550ms) لسعر الأونصة وجميع العيارات + **توهج أخضر/أحمر** عند كل حركة.
- **شريط أسعار متحرك (Ticker)** تحت الترويسة: أونصة، عيار 21، فضة، نسبة، تغيّر 24س — يتوقف عند التمرير، ويحترم `prefers-reduced-motion`.
- **مؤشر موضع السعر** داخل نطاق الفترة: شريط متدرج (أحمر←ذهبي←أخضر) بنقطة ذهبية متوهجة.
- **بطاقات 5 عيارات** (24/22/21 المميز/18/14) مع سهم اتجاه ومقدار التغير منذ بداية الفترة الحالية.
- **حاسبة أسعار فورية**: جرام/أونصة/**تولة** (لمن يتعامل بالهندية والسوق الخليجي) × العيار، مع أزرار أوزان شائعة (0.5/1/2.5/5/10) وتنبيه أن السعر قبل المصنعية.
- **حلقة عد تنازلي** حول زر «تحديث» توضح ثواني التحديث التلقائي (تتحول للحمراء قبل 5 ثوانٍ).
- **مخطط محسّن**: خط تقاطع عمودي عند التحويم، تلميح يعرض السعر والوقت والتغير منذ البداية، 4 مديات، وإحصائيات (أعلى/أدنى/تغير/عدد النقاط).
- **ترويسة أغنى**: اسم المحل «سبائك الفخامة» + العنوان، بدل الشعار فقط.

### ج. الاحترافية والهوية
- **PWA كاملة**: `manifest.webmanifest` + `sw.js` (الشبكة أولاً للصفحة، الذاكرة للأصول) + زر «تثبيت التطبيق» يظهر تلقائياً عند توفر المتصفح.
- **أيقونات**: `favicon.svg` (سبيكة ذهبية بتصميم خاص)، أيقونات 192/512 + maskable + Apple Touch من الشعار الرسمي بخلفية العلامة.
- **صورة مشاركة OG** 1200×630 حقيقية: الشعار الرسمي + «سبائك الفخامة» بخط Cairo + وسم العيارات — لتظهر بشكل فاخر في واتساب وتويتر.
- **تحسين محركات البحث**: `JSON-LD` (JewelryStore بعنوان النسيم — أسواق حجاب) + `og:locale ar_SA` + `canonical`.
- **مشاركة واتساب** بزر مخصص + مشاركة النظام + نسخ احتياطي.
- **اختصارات**: `R` تحديث، `S` مشاركة، `1-4` مدى المخطط (مع تلميح في التذييل).
- **إصلاحات**: ملف JPEG المسمّى PNG أُعيدت تسميته، شعار WebP موفّر للبيانات، حماية `matches`، إصلاح `ts/sourceUpdatedAt`.

### د. الأداء
- شعار 186KB → WebP 64KB للشاشات الكبيرة و26KB للجوال (مع `srcset` و`<picture>`).
- `preconnect` لواجهات الأسعار والخطوط.
- خط واحد فقط بوزنين مع `swap` (لا حجب للعرض).
- إيقاف المؤقتات عند إخفاء التبويب، ورفض التحديثات المتداخلة (`inFlight`).

### هـ. إمكانية الوصول
- رابط «تخطَّ إلى الأسعار»، `aria-live` لمناطق الحالة، `aria-label` للمخطط والأزرار، تباين محسّن، دعم كامل `prefers-reduced-motion`، وتركيز مرئي موحّد.

---

## 5) نتائج الاختبار الآلي

| السيناريو | النتيجة |
|-----------|---------|
| تحميل طبيعي: عرض أونصة/عيارات/فضة/نسبة/مخطط/تيكر/حاسبة | ✅ 37/37 |
| تفاعلات: تحديث يدوي، تبديل المديات، حاسبة (5 جرام، عيار 24>21)، لوحة مفاتيح، واتساب، تحويم المخطط | ✅ |
| وضع الفشل: انقطاع كل المصادر → آخر قراءة سليمة + حالة خطأ واضحة + ثبات بعد إعادة المحاولة | ✅ 9/9 |
| أخطاء وقت التشغيل (runtime errors) | صفر |

---

## 6) خارطة طريق مقترحة (أفكار مجانية إضافية)

1. **ترقية الـWorker** (مجانية ضمن Cloudflare Free):
   - إضافة مصدر Swissquote (bid/ask لحظي) وتغذية `/history` لمديات أطول (7/30 يوم).
   - إضافة نقطة `XAG` في نفس رد `/quote` لتقليل الطلبات.
2. **إشعارات واتساب/تيليجرام** عند تحرّك السعر ±0.5% خلال ساعة — عبر Worker المجاني (Cron Triggers).
3. **بوت تيليجرام** «@SabaaekBot» يرد بسعر اللحظة — نفس الـWorker مع Webhook.
4. **وضع فاتح (Light Mode)** عبر `prefers-color-scheme` مع الحفاظ على الذهبي.
5. **لغة ثانية** (English toggle) بنفس الملف عبر `data-i18n`.
6. **تحليل فني بسيط**: متوسط متحرك 7 أيام مرسوماً على المخطط (حساب محلي مجاني).
7. **سجل أسعار يومي** يُصدَّر CSV من الـWorker إلى Google Sheets (قالب [goldprice.dev/sheets](https://goldprice.dev/tutorials/google-sheets) مجاني).

---

## 7) ملاحظات جانبية

- الأسعار المتحقق منها وقت كتابة التقرير: XAU ≈ 4,358$ (gold-api.com) و XAG ≈ 63.1$، ونسبة ≈ 69.
- سعر الصرف SAR ثابت عند 3.75 (مرساة رسمية، وأكدها رد xaus.com نفسه).
- جميع ملفات الأيقونات وصورة OG مولّدة من الشعار الرسمي الحقيقي (لا إبدال للعلامة).
